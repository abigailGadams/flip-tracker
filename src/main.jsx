import React from 'react';
import ReactDOM from 'react-dom/client';
import FlipTimeline from './FlipTimeline.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FlipTimeline />
  </React.StrictMode>
);
